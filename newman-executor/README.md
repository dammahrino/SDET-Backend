# Newman Execution Service

## Description
Basic service used to execute the requests of the Client side. Locally stores the execution details.

## Set up
1. Install the dependencies with `npm install`
2. Start the service with `npm start` 

